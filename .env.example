# Copy to .env for local overrides. Vercel: set in dashboard/project env.
# Base path of the built app. Default "/" (Vercel). Set a sub-path for
# deploys under a repo URL (e.g. GitHub Pages): VITE_BASE_PATH=/TyperReflex
VITE_BASE_PATH=
